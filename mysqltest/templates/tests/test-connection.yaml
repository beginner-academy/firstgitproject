apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mysqltest.fullname" . }}-test-connection"
  labels:
    {{- include "mysqltest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mysqltest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
