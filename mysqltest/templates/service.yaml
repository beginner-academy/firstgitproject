apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysqltest.fullname" . }}
  labels:
    {{- include "mysqltest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mysqltest.selectorLabels" . | nindent 4 }}
